import React from "react";
import "./header.css";

const Header = () => (
  <header className="header">
    <h1>React Memory Game</h1>
    <h5>Click all 15 icons without repeating one!</h5>
  </header>
);

export default Header;
